import './globals.css';

export const metadata = {
  title: 'Blackgaur — Transport Management System',
  description:
    'Enterprise TMS for Indian transport: trips, fleet, FASTag tolls, fuel cards, RCM/GST invoicing, RBAC and analytics.',
};

export default function RootLayout({ children }) {
  return children;
}
